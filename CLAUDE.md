# kpi-compass-build-week

KPI Compass(月次売上CSV → 根拠つきブリーフ)のハッカソン成果物。`npm test` / `npm start` で動作。
`todo/` 配下は独立した「AI Todo」アプリ(下記参照)。

## クラウドセッション(スマホ/ブラウザ)から起動された場合

このリポジトリは Claude アプリの Code タブ(クラウドセッション)からも作業される。
そのとき環境はローカルと次の点で異なる(2026-09-20 実測)。詳細と運用テンプレは
`docs/mobile-remote-playbook.md`。

- **`~/.claude/CLAUDE.md` はクラウドセッションには存在しない。** このファイル内の記述だけを
  根拠に動く。なお ai-todo の正本は `~/.claude/CLAUDE.md` ではなく
  `~/.claude/skills/ai-todo/SKILL.md`(スキル)である(2026-09-20 ローカルで実測して訂正)。
  下の ai-todo 節はそのコピーで、2026-09-20 に同期済み
- `~/.claude/settings.json` も無い。個人の permissions / env は効かない
- computer-use / Claude in Chrome のツールは無い。PC のアプリ操作・ブラウザ実操作は不可
  (オーナーの実行環境は **Windows**。`C:\Users\anusu` / PowerShell。Mac ではない)。
  必要になったら ai-todo Issue に落とす
- Gmail / Google Calendar / Google Drive / GitHub の各コネクタは有効
- コンテナは無活動で回収される。**残すものは必ずコミット＋プッシュまで実行する**

## ai-todo: ユーザーのアクションアイテムを自動記録する

作業中に「ユーザー(Kota)本人にしかできないアクション」が発生したら、発生したその場で
GitHub リポジトリ `KotaHiguchiKH/kpi-compass-build-week` に、ラベル `ai-todo` 付きの Issue として記録すること。
ユーザーはGitHubのIssue一覧(ラベル ai-todo・番号の小さい順)でこの一覧を消化する。並べ替えない。自作AI Todoアプリは2026-09-17に停止し、todo/ は旧URLからの転送ページだけを置く。

対象の例: 承認・意思決定、アカウント作成や API キー発行、支払い、実機や本番環境での動作確認、
外部サービスでの手動設定、他者への連絡。エージェント自身で完了できる作業(コード変更など)は対象外。

### 起票前ゲート: まずオーナーに聞く

**「誰かに問い合わせて確認する」タスクは、Issue にする前に会話でオーナーに聞く。** オーナーは多くを
既に知っており、過去に AI と話した内容も含まれる。手元と会話履歴に無いことを確認してから起票する。

- 相手が役所・第三者(公的機関、企業の問い合わせ窓口、面識のない担当) → 従来どおり問い合わせて良い
- 相手が顔見知り(就活で会った社員、大学の教員、取引先) → 記憶喪失を疑われる社会的コストがあるため、
  既出の情報を再確認するだけの起票をしない
- ただし**確認せずに萎縮するのは禁止**。攻めの姿勢は維持したうえで、聞く前に頭を使う

### 納得ゲート(2026-08-14 追加・最優先)

**オーナーは納得できないタスクを実行できない。納得したタスクは実行できる。** Issue 化の前に
「オーナーがこのタスクの必要性に納得しているか」を必ず判定する。ここを飛ばして積むと消化されず、
滞留件数だけが増えてリスト全体の信頼性が落ちる(2026-08-14 に open 46 件を棚卸しし、11 件が
前提の崩れ・重複・実行窓の閉鎖で無効、8 件が是非未判断だったことを実測)。

- **オーナーが決めた方針から必然的に出るタスク** → 通常どおり Issue 化する
- **AI の判断・推奨に基づき、オーナーがまだ是非を判断していないもの** → タイトル先頭に `[要相談]`
  を付け、手順の代わりに**論点**(決めること・判断を左右する軸・推奨・決め方)を書く。
  アプリは `[要相談]` を期限判定の対象外にし、専用セクションへ振り分ける
- **前提となる判断が未決なら、行動 Issue を立てない。** 判断 Issue だけを `[要相談]` で立てる
  (「A社を受けるか」が未決なのに「A社に催促する」を起票しない)
- **前提が変わってタスクが不要になったら、その場でクローズする**
- **日付・期限が過ぎたタスクは棚卸しでクローズする**

本文の**冒頭**に必ず次の3行を置く。ここが書けないタスクは、そもそも Issue にしてはいけない。

```markdown
**なぜ:** <このタスクが何のゴールに効くか。1〜2文>
**やらないと:** <放置した場合に実際に起きること。締切や相手がいるなら明記>
**やらない選択肢:** <ある場合は書く。無いなら「なし(〜が確定しているため)」>
```

正本は `~/.claude/skills/ai-todo/SKILL.md`(ai-todo スキル)。齟齬があればそちらが優先する。
**このファイルはそのコピーであり、正本が読めない実行環境(Claude Code on the web のクラウドセッション等)
のために置いてある。** 正本を更新したらここも同じコミットで更新する。

### 本文フォーマット(レシピ形式・必須)

ユーザーは ADHD 特性があり、「手順を考える」「思い出す」「調べる」がタスク着手の障壁になる。
本文は 上記3行に続けて **上から順に実行するだけで完了するチェックリスト** として書くこと。
判断・調査・記憶を一切ユーザーに残さない(ただし「なぜ」は上の3行で必ず残す)。
アプリはこの形式を子チェックボックス・コピーボタン付きでレンダリングする。

ルール:

- タイトルは命令形で1行(例: 「Stripe の本番 API キーを発行して .env に設定する」)
- 手順は `- [ ] ` のチェックリスト。1ステップ = 1つの単純な操作(クリック、貼り付け、入力)
- 開くページは必ず **直リンク**(`https://...`)をステップ内に書く。「設定画面を開く」だけは禁止
- 実行するコマンドや AI に貼るプロンプトは、ステップの直後にコードブロックで置く
  (言語名 `bash` / `powershell` / `prompt` を付けるとアプリでラベル表示される)
- どこで何をするかまで指定する(「X をコピーして Y の Z 欄に貼る」)
- 末尾に以下のメタ行を入れる:
  - `_source: <プロジェクト名や会話の要約>_`(必須。アプリに発生元として表示される)
  - `_session: <このセッションのURL>_`(わかる場合のみ。アプリから元セッションに飛べる)
- 1アクション = 1 Issue。既に同じ内容の open Issue があれば作らない
- ユーザーから完了の報告を受けたら該当 Issue を close する

例:

````markdown
- [ ] https://dashboard.stripe.com/apikeys を開く
- [ ] 「本番キーを表示」をクリックして `sk_live_...` をコピーする
- [ ] 下のコマンドの `<キー>` を置き換えて、プロジェクトのターミナルに貼って実行する

```bash
echo "STRIPE_SECRET_KEY=<キー>" >> .env
```

- [ ] 完了したらこの Issue をチェックで消す

_source: ECサイト決済導入(Claude Code)_
_session: https://claude.ai/code/session_xxxx_
````

### 追加ルール(正本より。2026-09-20 同期)

- **並べ替えない。** 優先度・実行順を付けず、起票は末尾に積むだけ。急ぐものも順番を動かさず、
  締切48時間以内ならカレンダー通知を併用する。先送りは日付を指定する
- **完了報告の要否は起票時に Claude が決める**(オーナーに毎回判断させない。2026-09-16 決定)。
  結果が次の判断に効くもの(面談・応募・他者への連絡とその返事・条件や前提が変わりうるもの)は、
  最終ステップに `- [ ] Claude を開いて結果を一言報告する(<何を伝えればよいかの例>)` を置く。
  やって終わりのもの(支払い・設定・登録)は置かない
- **作成後にラベルが付いたか `gh issue view <n> --json labels` で必ず確認する。**
  `gh issue create -l ai-todo` はラベル付与に失敗しても URL を返して成功に見える。
  付いていないと GitHub の ai-todo 一覧に出ず実質届かないので、その場でオーナーへ伝える
- **Claude 自身がそのターン内で代行完了したら、その場で `gh issue close` する**

### 作成手段(使えるものを上から順に)

1. GitHub MCP ツール(issue の create)
2. `gh issue create -R KotaHiguchiKH/kpi-compass-build-week -l ai-todo -t "<title>" -b "<body>"`
3. `curl -sS -X POST -H "Authorization: Bearer $GITHUB_TOKEN" -H "Accept: application/vnd.github+json" https://api.github.com/repos/KotaHiguchiKH/kpi-compass-build-week/issues -d '{"title":"<title>","body":"<body>","labels":["ai-todo"]}'`
